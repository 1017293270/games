下面正文可直接复制给正在执行青云问道 Unity 迁移任务的智能体。

---

请继续你正在执行的“青云问道 Unity 客户端全面迁移、高度还原《一念逍遥》手游体验”任务，并纳入这份架构补充：

`/Users/zhuanzmima0000/Developer/work/games-unity3d/docs/architecture-supplement.md`

它补充实施重点，不缩减原来的完整目标。先读当前项目 AGENTS.md、client-ui-contract.md、migration-inventory.md、full-migration-official-reference.md 和 work-state.md，再用当前代码核实状态。项目文档目录为 `/Users/zhuanzmima0000/Developer/work/games-unity3d/docs`；work-state.md 和 AGENTS.md 在项目根目录。已完成的内容验证后复用，不按旧原型描述重复实现；历史进程号和旧验收报告也要重新核实。

本次调研已确认：我们的客户端是 Unity 6.6 + C# + UI Toolkit，使用 SpriteRenderer、圆形裁切 Shader 和正交相机表现水墨战场；后端复用并列 games 工程的 Node.js／TypeScript／Fastify 和 SQLite。服务器负责真实战斗与收益，Unity 接收全量／增量状态并呈现。这个分工继续保留。

《一念逍遥》官网安卓样本 `1.0.26081201` 的引擎标识是 `2022.3.34f1c1`，包中包含 IL2CPP、ToLua、Spine、DOTween、Timeline、CRIWARE、UWA、Bugly 和 ThinkingData。包内存在不代表所有功能都依赖这些组件；GS 的具体使用范围、网络协议、ECS 和热更新方案仍未确认。不要为追齐组件名单而替换我们的引擎、UI 系统、语言或数据库。

请依次推进下面两项，并与现有全面迁移工作衔接：

1. **改善 UI 的局部刷新。** 沿 Bootstrap 到 GameUi 页面和弹层的真实调用链，区分结构变化与普通数据变化。保持 UI Toolkit + USS，优先复用已有局部更新，对同页数值、气血、货币和按钮状态的变化更新对应控件，减少不必要的整页 Clear／Rebuild。原生数据绑定或保留控件引用直接更新都可以，选适合当前数据结构的较简单方案。不要为了采用 MVVM 另建大框架或全面重写页面。保留中文输入与焦点、滚动和选择、弹层、忙碌态、防重复提交、错误反馈，以及退出换号时的正确清理；旧回包不能污染新会话。

2. **让已有战斗表现可调、可保存、可预览。** 先实际查看对应的《一念逍遥》官方秘境战斗图片／视频，再用已有原创或授权资产，在圆球头像战场完成呼吸／护圈、施法、受击等代表效果的配置与预览。优先使用现有组件和 Unity 原生能力，把真实重复的表现参数从硬编码中提取出来，让颜色、时长、强度和曲线等修改后能保存、重运行和出包。需要的是有真实战斗调用的制作流程，不是空编辑器或另造完整时间线框架。服务器事件继续触发表现，保持自己／目标／Boss、气血、圆形裁切和 UI／场景层级清楚。

实施前先记录一个当前代码的简短基线，再直接执行已有授权范围内的工作，不在每个阶段重复请求确认。遇到资料不足或设备不可用时，明确受影响事项，继续推进不依赖它的工作；不要凭猜测补原作机制。

全程遵循项目已有边界：

- 所有游戏内容、画面、交互和动效改动前先查对应官方资料；记录链接、日期、采用点和本项目差异。静态截图不能证明动画周期；技术访谈的历史实现不能冒充当前版本。
- 保持平面水墨、清透留白和圆球包头像方向，保留未被否定的界面与交互；不要扩大为全三维重做或无关功能翻新。
- 真实战斗、消耗、掉落、挂机收益仍由 games 后端决定。保持切页仅停止观看、撤离才离场、断线恢复、全量／增量序列检查和重复施法事件的正确行为。不得用本地数值伪造成功，也不要为了动画修改服务器数值、概率或技能冷却。
- 只改本次需要的文件，保留已有未提交工作；不要改线上数据、部署服务或创建／删除工作树。沿用现有范围和公共契约，不追加通用门禁、架构禁令或新的审批流程。
- 网络长轮询、资源更新、音频诊断 SDK、Lua、数据库和服务拆分按补充说明登记后续需求；确有负载或发布依据再提出针对性方案。不要批量安装中间件来代替功能交付，也不要将延后项目标为已完成。

验收使用真实的新构建和业务结果。按修改内容选择现有 `npm run verify:unity`、`npm run verify:content`、`npm run verify:backend` 及 Mac／Web 构建，不把所有命令变成机械重复的检查。复用当前 ClientSmoke／client-fixture／连接专项，先读当前参数；共用 UI 改动跑真实核心流程，再补输入中收到更新、滚动中收到更新、弹层异步返回、退出重登等本次场景。使用隔离测试数据和新的证据目录，不抢占用户服务。

表现改动实际运行查看，并留下截图或视频：配置保存后仍生效，连续施法与受击正常，目标消失／死亡恢复／切图／断线重连无残留，UI 和战场关键信息可读。声称性能改善时给同条件对照数据。自动按钮事件、编译通过和旧 37 步报告都不能代替本次实际交互或手机真机证据。

请持续推进到上述两项有可审阅的实际结果，同时继续维护原有全量迁移范围。最终告诉我：主要改动与官方依据、实际运行验证和证据路径、可以打开的新构建、仍未完成的具体事项。这两项优化通过不等于全部手游高还原验收通过。
