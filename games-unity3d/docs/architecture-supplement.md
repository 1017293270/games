**青云问道 Unity 架构补充说明：对照依据、改进重点与验收方式**

编写日期：2026-09-07（洛杉矶时间；UTC 为 2026-09-08）。状态：待执行的补充说明，不是实现完成或验收通过声明。

本文件补充现有“全面迁移、高度还原《一念逍遥》手游体验”的任务。完整功能范围继续以当前用户指令和迁移盘点为准；本文件提出的客户端优化不能代替全量功能交付。执行前复核当前代码，已经完成的事项直接验证，避免重复改造。

**先理解现有分工。** Unity 工程在 `/Users/zhuanzmima0000/Developer/work/games-unity3d`；它复用 `/Users/zhuanzmima0000/Developer/work/games` 的游戏后端和 shared 内容。当前链路为：UI Toolkit 页面 → Bootstrap 会话与请求协调 → REST／Socket.IO → Node.js 游戏服务与模拟 → SQLite；实时状态返回后，由 ZoneState 接收全量／增量帧，WorldView 插值并呈现战场。

这是原生 Unity 玩家界面。服务器负责真实移动、选敌、战斗结果和奖励；客户端可以平滑位置、播放动作及回放战报，不得自行裁定伤害、消耗、奖励或伪造服务器事件。

| 对照项 | 本项目的代码现状 | 《一念逍遥》的证据 | 实施含义 |
| --- | --- | --- | --- |
| 引擎 | Unity `6000.6.0f1` | 官网安卓样本标识 `2022.3.34f1c1` | 保持现有引擎；版本号不代表同场景性能结论 |
| 客户端脚本 | C#；当前 Standalone 构建配置为 Mono | 安卓包包含 C# 程序集、IL2CPP、ToLua | 不因组件不同引入 Lua；手机构建后端随实际发布目标确定 |
| 界面 | UI Toolkit + USS，C# 动态创建控件；Rebuild 会清空并重建页面，部分区域已有局部更新 | 包内包含 UGUI／TextMeshPro；2022 年访谈披露 LuaWindow 和类似 MVVM 的数据绑定 | 优先改善局部刷新和状态保存，不要求换 UI 系统 |
| 场景与动效 | SpriteRenderer、正交相机、圆形裁切 Shader、LineRenderer；多种动效参数写在 WorldView 中 | 包内包含 Spine／DOTween／Timeline；历史访谈披露运行状态下的战斗表现编辑器 | 把当前表现做成可调、可预览的制作流程；中间件按真实需求选用 |
| 网络 | REST + Engine.IO 4／Socket.IO HTTP 长轮询，JSON 全量／增量状态 | 实际协议、同步算法未公开 | 保留现有事件契约；优化前测量，不能假定原作使用某种协议 |
| 服务端 | Node.js 24+、TypeScript、Fastify；单进程内维护多个地图模拟 | 官方说明离线角色继续模拟；公司有 GS 框架，游戏当前使用范围未确认 | 继续复用现有权威模拟，不另造 Unity 服务端或自研语言 |
| 数据 | SQLite 单文件、单连接、WAL、事务和迁移 | 历史官方案例采用阿里云 MongoDB、副本集、业务重试及缓存 | 数据库差异不直接构成迁移理由，按负载与恢复要求评估 |
| 资源与运营 | shared 导出 catalog 与资源，Resources 加载；当前未见远程资源更新流程及完整音频／第三方诊断接入 | 包内包含 CRIWARE、UWA、Bugly、ThinkingData；具体热更新方案未核实 | 将相关能力保留在完整交付差异清单，避免把安装 SDK 当作能力验收 |

当前源码还明确区分“停止观看”和“撤离”：切页触发 zone:leave，仅取消观看；zone:retreat 才结束驻留。断线后的角色继续执行秘境逻辑，达到配置的离线上限才移出。源码默认上限为 12 小时，并有离线 PvP 保护；这是本项目规则，实际运行配置可能不同，不能描述成原作标准。

**对照证据如何使用。** 下列材料已经在本次调研中读取，后续修改的系统、版本或证据条件变化时仍需重新核实。

| 证据 | 来源、时间与确认范围 |
| --- | --- |
| 官方安装包静态检查 | 从[官网](https://xian.leiting.com/)取得 `https://yinxydlf.leiting.com/full/xian_M161573.apk`；包名 `com.leiting.xian`，版本 `1.0.26081201`；CDN Last-Modified 为 2026-08-14。分段读取 ZIP 目录及元数据，提取元数据的 CRC32 一致。未安装运行，也未完成完整包签名认证 |
| 引擎及程序集 | `globalgamemanagers` 含 `2022.3.34f1c1`；目录有 `libunity.so`、`libil2cpp.so`、`libtolua.so`；程序集清单含 `spine-unity.dll`、`DOTween.dll`、`Unity.Timeline.dll`。这是包内存在的证据，不证明每个功能的运行时调用 |
| 客户端设计访谈 | [Unity 官方平台署名访谈](https://indienova.com/indie-game-development/unity-yinianxiaoyao-interview/)，2022-03-02 刊载。披露 UI／光效排序、Lua 界面绑定及战斗编辑器；当时引擎为 2018.4.13f1，不能将旧实现直接当作当前全部代码 |
| 离线模拟 | [官方入驻的 TapTap 游戏介绍](https://www.taptap.cn/app/159465)，本次读取的页面说明角色离线后仍在服务器战斗、产出及交互；未公开调度频率和驻留时限 |
| 数据库 | [MongoDB 官方客户案例](https://www.mongodb.com/zh-cn/solutions/customer-case-studies/g-bits)。同题[阿里云文章](https://developer.aliyun.com/article/879263)标注 2022-03-25；属于历史架构资料，不能证明当前全部部署参数 |
| GS 语言 | [吉比特官方公告](https://www.g-bits.com/zh/news/591.html)，2022-04-26。确认公司自研编译器、服务器框架及公共服务，不能推出游戏当前所有服务都使用 GS |
| 可复用的引擎能力 | [Unity 运行时数据绑定说明](https://docs.unity3d.com/cn/6000.0/Manual/UIE-get-started-runtime-binding.html)，适用版本 6000.0+。支持将控件属性绑定到数据源，按源变化更新 |

详细研究材料可见[调研报告](/Users/zhuanzmima0000/.codex/visualizations/2026/09/08/01a07e95-df57-7511-af3a-56ab10a05c81/yinianxiaoyao-architecture-report.md)和[APK 核验记录](/Users/zhuanzmima0000/.codex/visualizations/2026/09/08/01a07e95-df57-7511-af3a-56ab10a05c81/yinianxiaoyao-apk-evidence.json)。核心结论已写入本文件，外部本地附件仅供追溯。

UI Layer／LayerDiff 是对方历史实现的设计线索，不要求照搬同名组件。GS、网络协议、ECS、具体渲染管线和热更新机制的未知部分继续标为待核实；不能用行业惯例补成原作事实。JSON 文档数据库不证明网络用 JSON，缓存不等于 Redis，包含 Entities 不证明核心战斗使用 ECS。

**优先工作一：让普通数据变化更新现有控件。** 从当前 Bootstrap → Rebuild → GameUi 页面／弹层的真实调用链入手，区分页面结构变化、列表变化和数值变化。

- 保留现有 UI Toolkit、USS、页面入口、交互风格和已修好的输入、滚动、通知、弹层行为。先选择实际高频刷新区域完成端到端改进，再覆盖相同问题的其他调用路径。
- 数值、气血、货币、忙碌态或按钮可用性改变时，优先更新对应控件；确实改变结构时才重建相关区域。聊天增量和已有局部更新可以继续复用。
- 原生数据绑定与“保存控件引用后更新 text／value／style”都可以采用，选择适合现有 JObject 数据的较简单方案。不为接入绑定而强制增加整套 DTO／ViewModel／状态管理框架，也不要求将所有动态页面转写为 UXML。
- 保留有效的输入焦点、中文组合输入、光标、列表选择、滚动位置和弹层状态；注销或换角色时正确清除旧状态与敏感输入。解绑失效订阅，防止旧响应或旧账号数据刷新新页面。
- 复用 Command 的防重复提交、错误展示和会话代际判断；界面优化不能引入扣款请求的自动重试或让真实失败显示为成功。

完成应有实际证据：同一页面结构下，服务器数值更新不触发不必要的整页清空；编辑输入或滚动列表时到达推送仍保持操作状态；提交后真实服务端数值与界面一致。切页、关闭弹层、退出重登和失败响应也应验证。若讨论性能收益，记录同一构建条件和同一业务场景下的重建次数、耗时或分配变化，不能凭代码变短宣布更快。

**优先工作二：让已有战斗表现可配置、可预览。** 目标是让颜色、持续时间、强度、缩放／轨迹等常用表现参数可调并能保存，不要求自研完整时间线系统。

- 先按项目 AGENTS.md 实际查看对应官方秘境战斗图片或视频，记录来源、日期、可观察的表现，以及项目自定部分。静态图不提供动画周期依据。
- 在当前圆球头像战场中，选已有的呼吸／护圈、施法、受击等代表效果完成可用流程，再提取实际重复部分。优先利用现有组件、可序列化配置、曲线或合适的 Unity 原生工具；仅在必要时引入一个有明确用途的配置资产。
- 既有服务器状态／事件继续驱动表现。网络状态中的 full／delta、序列号、重复施法脉冲、伤害及收益契约保持正确；表现时长不能反向决定服务器技能冷却或结算。
- 分别理清 UI Toolkit 面板、场景 SpriteRenderer／LineRenderer、肖像与护圈的前后关系，确保自己、当前目标和 Boss 易辨认，特效不遮挡关键气血和操作。保留圆形裁切、受击闪烁、目标消失、实体槽位复用等已有正确行为。
- 配置应能在实际播放状态中预览，保存后重新运行及构建仍生效。不要把临时 Inspector 数值、脱离真实事件的演示片段或仅有空面板的编辑器当作交付。
- 原创或已有授权资产继续使用。Spine 需要配套动画资产，DOTween／Timeline 需要真实使用场景；本文件不要求采购或一并安装这些组件，也不要求改成全三维场景。

完成应包含：一套已接入真实战斗的可调表现；调整并保存参数后的实机或实际客户端片段；自己／目标／Boss 同时出现时的可读性检查；连续施法、受击、死亡／恢复、目标消失、切图和断线恢复时无陈旧效果残留的证据。视觉效果与流畅性以实际运行结果判断。

**后续按需求评估的项目。** 以下差距继续登记，不自动扩成本次重写任务。

| 项目 | 何时值得推进 | 先取得的依据 |
| --- | --- | --- |
| WebSocket 或其他传输 | 长轮询的消息开销、延迟或目标平台限制确实影响体验 | 同场景收发量、延迟及重连结果；保持既有事件／恢复语义的方案 |
| 远程资源更新／Lua | 发布频率、包体或客户端逻辑更新确有需求 | 当前代码与内容的发布成本、目标平台限制、回退及版本兼容要求；不把 ToLua 的存在当作对方完整热更新证明 |
| 数据库及服务拆分 | 写入阻塞、容量、恢复时间或多节点运行需求出现 | 真实负载、事务耗时、故障恢复结果；数据库类型与服务拆分分别论证 |
| 音频与诊断 SDK | 完整体验需要音效，或发布后故障定位需要统一采集 | 先用项目已有或 Unity 原生能力完成有用结果，再判断是否需要 CRIWARE／UWA／Bugly 等；接入后验证实际功能 |
| Android／iOS 构建和真机 | 进入相应移动端交付工作时 | 按目标平台检查输入、竖屏、后台恢复、内存／帧时间和长时运行；Mac 的 Mono 配置不代表手机发布配置 |

**修改边界沿用现有授权。** 此文件不增加新的审批流程，也不授权改线上数据、部署服务、重写其他系统或缩减原任务。仅改实现上述目标所需文件，保留已有未提交工作；需要调整公共契约时，说明最小变更并同步相关调用方。不要自行创建或删除工作树。

执行时阅读当前 [AGENTS.md](/Users/zhuanzmima0000/Developer/work/games-unity3d/AGENTS.md)、[客户端契约](/Users/zhuanzmima0000/Developer/work/games-unity3d/docs/client-ui-contract.md)、[迁移盘点](/Users/zhuanzmima0000/Developer/work/games-unity3d/docs/migration-inventory.md)、[官方对照文档](/Users/zhuanzmima0000/Developer/work/games-unity3d/docs/full-migration-official-reference.md)和 [work-state.md](/Users/zhuanzmima0000/Developer/work/games-unity3d/work-state.md)，再核实实际源码。旧文档仍可能描述 OnGUI 原型或旧进程；当前代码已经是 UI Toolkit，历史端口／进程号也不能直接拿来操作。

**验证复用现有工具，按改动选择。** 工作目录为 `/Users/zhuanzmima0000/Developer/work/games-unity3d`。

| 变更 | 可复用的检查或命令 |
| --- | --- |
| C#、Shader、帧缓存或编辑器配置 | `npm run verify:unity` |
| catalog、同源表或资源映射 | `npm run verify:content`；确实需要重新导出时才运行 `npm run content` |
| 服务端或实时连接契约 | `npm run verify:backend`，以及现有连接专项／相关后端测试 |
| 交付当前 Mac／Web 客户端 | 按受影响目标运行 `npm run build:mac`、`npm run build:web`，并实际运行新构建 |
| 共用 UI 或交互 | 复用 ClientSmoke 与 client-fixture 的真实核心流程，补本次缺口；同时做实际鼠标／触控路径检查 |

先读当前测试工具的入口参数，不能把上述命令表当作每次必须全部执行的新门禁。优先使用现有隔离 fixture 和新证据目录；识别当前运行服务，避免抢占用户进程或覆盖现有玩家存档。Unity Editor 和同一工程的构建顺序协调，避免同时写入工程和争抢锁。

已有“37 步通过”等记录属于其当时的构建，不能自动给本次变更背书；NavigationSubmitEvent 通过也不证明鼠标命中、中文输入或真机性能。需要补测时留下最小可复现检查，避免另建庞大测试框架。原有边界场景和全量目标继续保留，设备或官方资料不足的项目单独注明，已完成的其他工作照常归档和推进。

执行汇报说明：改了什么、依据是什么、真实行为如何验证、产物与证据在哪里、哪些尚未完成。UI 优化和表现配置完成只代表这两项完成，不能据此宣布全部手游迁移或原作高还原已经验收。
