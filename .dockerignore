# 构建镜像时不发送给 Docker 的东西。
# 目标：只把「源码 + 素材 + 锁文件」送进去，产物一律在镜像里重新构建。

# 依赖与构建产物 —— 镜像内会重新 install / build，宿主机的版本反而可能污染
**/node_modules/
**/dist/
**/build/
**/coverage/
**/*.tsbuildinfo
**/.vite/
**/.vitest/

# 运行时数据。数据库靠 docker 卷持久化，绝不能烤进镜像
**/data/
*.db
*.db-shm
*.db-wal
*.db-journal

# 环境变量：镜像里不带任何密码
**/.env
**/.env.*
!**/.env.example

# 版本库与编辑器
.git/
.gitignore
.github/
.idea/
.vscode/
.claude/
**/.DS_Store

# 素材管线的中间产物（apps/client/public/art 里的成品要保留，这里排的是原始大图）
tools/art/raw/
.imagegen-logs/
apps/client/shots/

# 只给人看的东西
docs/
*.md

# 测试与产物报告
e2e/tests/
**/playwright-report/
**/test-results/

# 日志
*.log
npm-debug.log*
